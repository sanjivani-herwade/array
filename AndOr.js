let temp = 85

if (temp <= 60 && temp <= 90) {
    console.log('it is freezing outside')
} else if (temp >= 8 || temp >= 130) {
    console.log('it is hot outside')
} else {
    console.log('it is preety')
}